import { LanguageCode } from '@matjar/common/lib/generated-types';

export const DEFAULT_LANGUAGE = LanguageCode.en;
export const DEFAULT_LOCALE = undefined;

export const DEFAULT_AVAILABLE_LANGUAGE_CODES = [
	LanguageCode.ar,
	LanguageCode.en,
];

export const DEFAULT_AVAILABLE_LOCALES = [
	'AR',
	'US',
];
